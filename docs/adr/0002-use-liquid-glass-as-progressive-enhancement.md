# Use Liquid Glass as Progressive Enhancement

The app will use native Liquid Glass styling on iOS versions that support it, while keeping equivalent non-glass SwiftUI fallbacks for older iPhones. This preserves the desired modern Apple feel without excluding the listener's likely device base.
