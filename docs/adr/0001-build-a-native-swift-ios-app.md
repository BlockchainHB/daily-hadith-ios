# Build a Native Swift iOS App

This app will be built as a native Swift iOS app so the listening experience can feel calm, polished, and familiar to iPhone users, with system audio behavior and Apple-style navigation. The trade-off is that the first version targets iOS only instead of using a cross-platform framework.
